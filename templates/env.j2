#!/bin/sh

export PATH="{{ ansible_env.HOME }}/.plenv/bin:$PATH";
export PLENV_ROOT="{{ ansible_env.HOME }}/.plenv";
export HOME="{{ ansible_env.HOME }}"
eval "$({{ ansible_env.HOME }}/.plenv/bin/plenv init -)";

cd $(dirname $0)

exec "$@"
